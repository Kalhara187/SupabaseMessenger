{
  "name": "SupabaseMessenger",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:mobile": "cd mobile && npx expo start",
    "start": "npm run start:mobile",
    "build:android:apk": "cd mobile && eas build -p android --profile preview"
  }
}
